- type Shape = {
  name: string;
  color: string;
};

- interface Shape {
   name: string;
  color: string;
}

- interface có thể merge còn type thì không.
- Type có union type còn interface thì không
